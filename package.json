{
  "name": "@hitz-group/dynamo-helper",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "ssh://github.com/hitz-group/tillx.git"
  },
  "scripts": {
    "build": "yarn  clean && yarn  compile",
    "build:watch": "yarn compile -- -w",
    "build:test": "tsc -p ./tsconfig.build.json",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -p ./tsconfig.build.json",
    "test": "yarn run build:test && yarn run test:only",
    "test:only": "jest"
  },
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "2.725.0",
    "lodash": "4.17.19",
    "uuid": "8.3.0"
  },
  "devDependencies": {
    "@babel/core": "^7.11.1",
    "@babel/preset-env": "^7.11.0",
    "@babel/preset-typescript": "^7.10.4",
    "@types/jest": "^26.0.9",
    "babel-jest": "^26.2.2",
    "jest": "^26.2.2",
    "ts-jest": "^26.1.4",
    "typescript": "^3.9.7"
  }
}
